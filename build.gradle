plugins {
    // Define the Android Application plugin version and apply it in the module-level build file
    id 'com.android.application' version '8.4.0' apply false

    // Define the Kotlin plugin version for Android
    id 'org.jetbrains.kotlin.android' version '1.9.23' apply false
}
